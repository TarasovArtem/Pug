# -----------------------------------
#
#              --FIRST TESTS--
#
#------------------------------------
name: First_test

on: 
  push: 
    branches:
      - master

jobs:
  First_test: 

    runs-on: ubuntu-latest

    steps:
    - name: Print message
      run: echo "First test"
